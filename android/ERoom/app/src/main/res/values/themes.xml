<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.MyTheme" parent="Theme.AppCompat">
        <!-- for the default action bar background -->
        <item name="colorPrimary">@color/primary_material_dark</item>

        <!-- for the status bar -->
        <item name="colorPrimaryDark">@color/primary_dark_material_dark</item>

        <!-- the default value for colorControlActivated, which is used to tint widgets -->
        <item name="colorAccent">@color/accent_material_dark</item>

        <!-- You can also set colorControlNormal, colorControlActivated, colorControlHighlight,
            and colorSwitchThumbNormal. -->
    </style>
</resources>